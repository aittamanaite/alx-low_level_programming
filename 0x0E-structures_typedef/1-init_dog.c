#include "dog.h"
/**
 * init_dog - function void
 * @d: pointer
 * @name: parameter
 * @age:parameter
 * @owner: parametere
 * Return : 0(success)
*/
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d)
	{
		(*p).name;
		(*p).age;
		(*p).owner;
	}
}
